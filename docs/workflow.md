# 作業の進め方

改訂: 2026-07

aozora2 の保守・開発の作業手順を定める。以後の作業は本書に従う。

手順は原則（docs/architecture.md 1 章: 荷重原則・誘因整合の原則）から
導かれている。手順が実態と合わなくなったら、原則に立ち返って手順を組み直し、
本書を更新する。本書に書かれたコマンドは実行して検証できる——動かなく
なっていれば、それ自体が本書の古さの検出である。

## 0. 前提となる配置

```
aozora/
├── aozora2/          # 本体（このリポジトリ）
├── aozora2html/      # 参照実装（Ruby）。挙動の実測に使う
├── aozorabunko/      # コーパス（cards/ 以下に作品）
└── aozora-htmlcheck/ # 適合性ハーネス・オラクルアーカイブ（oracle/）
```

## 1. 全作業共通の不変条件

どの種類の作業でも、コミット前に以下をすべて満たす。

1. 全テストが通る:
   ```
   cd aozora2 && cargo test
   ```
2. 適合性の回帰がない（悪化 0）:
   ```
   cd aozora-htmlcheck && cargo build --release
   ./target/release/aozora-htmlcheck --oracle-dir oracle \
       --baseline baseline-oracle.jsonl --out results-oracle.jsonl
   ```
   **重要**: ハーネスは aozora2 を `aozora2::html::convert` としてライブラリ
   リンクする。aozora2 側だけ `cargo build` してもハーネスのバイナリには
   反映されない。必ず `aozora-htmlcheck` ディレクトリで `cargo build --release`
   を実行してから走らせること（さもないと旧コードで比較し、悪化0 が偽になる）。
   改善が出たら baseline を更新し、同じコミットに含める:
   ```
   ./target/release/aozora-htmlcheck --oracle-dir oracle \
       --baseline-out baseline-oracle.jsonl --out results-oracle.jsonl
   ```
3. 挙動を意図的に変える場合、その変更は次のどちらかである:
   - 互換性の改善（オラクルへの接近）
   - Quirk フラグ配下の切り替え可能な挙動
4. コミットメッセージに「何を・なぜ・参照実装/注記一覧のどこに対応するか」を
   過去形で書く。コミット履歴は腐らない一次記録である。
5. コメントに規範を書かない。規範はテストへ。コメントは参照ポインタまで。

## 2. ループA: 互換性修正（オラクルとの差を埋める）

2025-26 の互換性改修（byte 一致 78%→98%）で実証された手順。

1. **観測**: ハーネスを回し、残差を種類別にクラスタリングする
   （results-oracle.jsonl の diffs を kind × path で集計し、件数と代表例を出す）。
2. **診断**: 代表例の最初の相違箇所とソース行を突き合わせる:
   ```
   cd aozora-htmlcheck && ./diagnose.sh <対象zip>
   ```
3. **実測**: 参照実装に最小入力を与え、期待挙動を確定する。**推測で実装しない**:
   ```
   cd aozora2html && ruby -Ilib bin/aozora2html 入力.txt 出力.html
   ```
   最小入力は CP932・CRLF で作る。例:
   ```
   python3 -c "open('t.txt','wb').write('題\n著\n\n本文\n\n底本：「甲」乙\n'.replace('\n','\r\n').encode('cp932'))"
   ```
4. **固定**: 期待挙動をテストに書く（名前だけで規則が分かるように）。
5. **修正**: 実装を直す。
6. **計測**: 不変条件 1・2 を確認する。「改善 N・悪化 0」以外の結果になったら、
   原因を潰すか revert する。悪化を抱えたまま先へ進まない。
7. **記録**: コミットする（不変条件 4）。

進め方のコツ:

- 一度に 1 規則。悪化 0 を絶対に崩さない。
- 効果（何件直るか）で優先順位を付ける。差分クラスタの件数がそのまま見積りになる。
- 参照実装の Ruby コードを読むときは、対応する実測を必ず添える
  （コードの読み違いは実測で発覚する）。

## 3. ループB: 参照実装のバグ・実装都合に遭遇したら（Quirk 化）

**判定基準**——いずれかに該当すれば Quirk:

- 規格上不正な HTML を生む（例: 属性値の中にタグが入る）
- 無意味な出力（例: 空の `width="" height=""`）
- 記法の使い分けとして作者が意図しえない差
  （例: bare「字下げ終わり」と「ここで字下げ終わり」の `<br/>` 差）

該当しない出力差（レイアウトの流儀・改行数の設計など）は通常の互換実装として
ループAで扱う。

手順:

1. `Quirks` にフラグを追加する（既定オン＝参照実装一致）。
2. on / off 両方の挙動をテストで固定する（ペアテスト必須）。
3. architecture.md 5 章の決定記録に日付つきで追記する。

## 4. ループC: 挙動不変リファクタリング

1. 大規模な場合は事前に全件出力スナップショットを取る:
   ```
   cd aozora-htmlcheck && python3 snapshot.py /tmp/snap_before.txt
   ```
   完了後に再取得して byte 一致を確認する。
2. 小さく刻み、各段で不変条件を確認してコミットする。
3. 方向は architecture.md 2 章・4 章へ向かうものに限る:
   型の壁を強める / 規範をテストへ移す / 文字列詮索を減らす / 1 規則 1 箇所へ寄せる。
4. 迷ったら revert。リファクタリングは機能を増やさないので、
   失敗を抱え込む価値がない。

## 5. ループD: 三者照合（注記一覧・コーパス・実装のドリフト対応）

三者は完全に一致せず、それぞれ更新されながら共進化する。照合は恒常作業である。

**未対応記法の検出**: 未実装・未解決の記法は、出力に
`<span class="notes">［＃…］</span>`（注記としての素通し）として必ず表面化する。
生成 HTML からこれを集計すれば、未対応記法の一覧と頻度が得られる。
件数上位から、注記一覧・コーパス実例・参照実装の挙動を照合して対応を決める。

**コーパスが更新されたら**: 新規・改版作品をハーネスの対象に加える
（`build_oracle.sh` は生成済みをスキップするので増分で回る）。
処理不能・未対応記法・回帰が出れば上記とループAで処理する。

**注記一覧が更新されたら**: 差分を読み、実装挙動・コーパス実態と照合する。

- 参照実装が既に対応している記法 → 実測（ループA手順 3）で期待挙動を確定する。
- どの実装も未対応の新記法 → 注記一覧の記述とコーパス実例から期待挙動を決め、
  根拠を決定記録に残し、テスト → 実装の順で作る。

**矛盾したら**: 既定則は architecture.md 3 章
（互換モードの挙動は参照実装が勝つ / バグは Quirk へ / 公開 HTML は権威にしない）。
既定則から外れる判断をしたときは必ず決定記録に残す。

## 6. 知識の置き場所

| 知識 | 置き場所 |
|---|---|
| 挙動の規則 | テスト（規則が読み取れる名前を付ける） |
| なぜそうしたか | コミットメッセージ（過去形）・architecture.md 5 章の決定記録 |
| 設計方針の変更 | architecture.md の本文改訂＋決定記録への追記 |
| 作業手順の変更 | 本書の改訂 |
| コメント | 参照ポインタのみ。規範を書かない |

テストの書き方:

- 名前だけで規則が分かるようにする
  （例: `test_kokode_close_suppresses_break_but_bare_does_not`）。
- 期待値は参照実装の実測から取る（ループA手順 3）。
- Quirk はペアテスト必須（ループB手順 2）。

### 命名・同居の規約（記法語を正準トークンにする）

architecture.md 2.1 性質5「ある記法の定義・意味・実例が grep 一発で揃う」を
満たすための規約。**結合キーは記法語そのもの**——`［＃…］` に現れる日本語
（`傍点`・`中見出し`・`返り点` など）を正準トークンとする。理由: 記法語は
コーパス・注記一覧・参照実装・本実装のすべてに共通して現れる唯一の語彙であり、
英名（`SesameDot` 等）は内部都合で揺れる。

規約:

- 記法の**定義**（パース分岐・記法語→型のマッピング）は記法語の文字列を
  リテラルで含む。
- 記法の**実例**（テスト）は入力文字列に記法語をそのまま書く。テスト名は
  英名でよいが、本文に記法語が無いと grep から漏れる。
- 将来のデータ表の**行**も記法語をキーに持つ（データ駆動化の前提）。

現状: `grep 傍点` で定義・レンダリング・テスト保有ファイルが揃う（記法語は
テスト本文の入力に必ず現れるため）。この性質はテスト名の英名化では壊れないが、
入力を変数へ追い出す・記法語を定数に逃がす等をすると壊れる。壊さないこと。

同じ対応表を二箇所に書く場合（例: `StyleType::from_command` と
`command_name`）は往復ガードのテストで縛り、片方だけの改修を検出可能にする
（誘因整合）。最終的にはデータ駆動化で一表へ畳む。

## 7. 検証インフラの維持

- インフラ（ハーネス・オラクル・baseline）が壊れたら、他の作業より先に直す。
  守衛が死んだ状態で作業しない。
- オラクルを再生成・追加したときは、来歴（生成日・参照実装のリビジョン）を
  `oracle/` に添える。
- baseline の更新は「改善をコミットするとき」に行う（不変条件 2）。

## 8. 現在の作業キュー

architecture.md 6 章「移行の段階」の順序に従う。

**コメント→テスト監査（2026-07 実施済み）**: 参照実装の挙動を説明する規範
コメント約 60 箇所を、対応するテストの有無で照合した。結果、大半は既に
テストで固定されていた（互換性改修時に積んだ約 50 本の効果）。耐久層の穴
だった 4 規則をテスト化した（空ルビ・送り仮名欠きの見出終わり・入れ子「」の
注記ルビ・写真 photo クラス）。
なお規範を運ぶコメントの一律「薄化」は行わなかった。現存コメントは主に
「なぜ（参照実装のどこに対応するか）」を説明する案内であり、規範自体は
テストへ移っている。案内コメントを機械的に `see test X` へ置換すると
comment→test 名の脆い結合を生むだけで、荷重原則上の利得が無いと判断した
（腐敗リスクのある「規範の主張」を含むコメントがあれば個別に是正する）。
一部の細則（面-区-点画像の JIS X 0213 項目・前方参照の判定順）は入力構築が
煩雑なため単体テスト化せず、オラクル（各数百件規模）での保護に留めた。

**命名・同居の規約（2026-07 実施済み）**: 記法語を正準トークンとする規約を
§6 に明文化。あわせて第一対象として `StyleType` の記法語↔バリアント二重表を
単一台帳（command_name を真実源とし ALL_STYLE_TYPES 上で逆引き）へ畳んだ。

**コーパス更新の反映（2026-07-24 実施, ループD）**: aozorabunko を最新化
（fe49dbf16e）。zip がオラクル gz より新しい 10 作品を陳腐化として再生成、
新規 56 作品を追加。baseline を更新し悪化0 に収束（新規 55 は exact）。
oracle/PROVENANCE.txt に来歴を記録。

**既知差 002406/62767『緑の怪物』（2026-07 解消済み, 段2c）**: burasage
ブロック内の見出し行が行末で `</div>` を閉じる参照の癖。
`presentation::is_midashi_line` を追加し renderer.rs の burasage 分岐で
見出し行に `</div>` を付与して解決（byte 一致）。テスト
`test_midashi_inside_burasage_closes_an_extra_div`。

**記法のデータ駆動化（2026-07 実施済み）**: 参照実装が持つ3つのデータ
（accent_table.yml・jis2ucs.yml・command_table.yml）を、生成 or 照合で
参照側に紐付けた。accent と jis2ucs は既に build.rs で JSON から生成済み。
今回 command_table.yml をスナップショット（data/command_table.tsv）として
取り込み、presentation.rs の css/tag を照合するテストを追加（左/上バリアントは
参照の方向フィルタで導出して照合、StyleType::all() 全網羅）。あわせて濁点
片仮名表の二重定義を Node::dakuten_katakana_char へ集約。
なお BlockType・FontSizeType・MidashiLevel/Style は「順序依存の contains
ディスパッチ」であり、フラット表化すると順序意味（折り返して＞字下げ 等）や
レベル抽出を失うため、データ駆動化の対象外として現状の分岐で残す。

**型の壁・文字列詮索の排除（2026-07 一部実施, 段2b/2c）**:
- 段2b: renderer.rs の行単位地付き判定を文字列詮索から AST（先頭ノードが
  行スコープ Chitsuki か）へ置換。renderer.rs から記法文字列の再詮索が消えた
  （残る `line` 参照は空行判定と scan_kunoji のみ）。
- 段2c: 上記 burasage 既知差を解消。

**行分類の凝集（2026-07 実施, 段2c）**: render ループに散在していた
出力HTML詮索3つ（classify_line / is_midashi_line / is_block_only_line）を
`presentation::classify_output_line` が返す型付き `LineInfo` に一本化。
ループは型付き信号を消費し、分類ロジックは一箇所に凝集した。

**残った深い構造変更についての判断（保留）**: BlockManager 除去・ブロックの
AST サブツリー化・出力HTML詮索の完全なノード化は、着手前の精査の結果、
現時点では**着手しない**と判断した。理由:
- ドリフトの本丸だった「記法ソースの再詮索」は段2bで除去済み。残る出力HTML
  詮索（`is_block_only_line` 等）は分類器がレンダラと共に変化するため独立した
  ドリフト源ではなく、荷重原則上の優先度が低い。
- `is_block_only_line` は同行/窓見出し除外・単一タグ・アンカー検出など多数の
  `<br/>` 抑制エッジを符号化しており、AST から等価に再導出するのは高リスク。
  98%超の byte 一致を崩す危険が、得られる構造的利得に見合わない。
- BlockManager 自体は 169 行の素直なスタックで、単体では有害でない。
着手する契機は「具体的な正しさの欠陥（現状の詮索では再現できない参照挙動）が
出たとき」。そのときは `classify_output_line` / `LineInfo` を継ぎ目として、
該当行種だけをノード由来判定へ差し替える（全面書き換えではなく局所置換）。

**残差クラスタリングとループA（2026-07 実施）**: different 328件を「最初の
相違の前後を数値・非ASCIIを正規化した署名」でバケット化して観測した
（手順は下記「残差観測」）。最大クラスタ群は burasage（折り返し字下げ）行の
包み方の差で、複数バケットに分散して合計で最多。次点にヘッダのエスケープ差、
末尾 `<br/>` 数、gaiji 一覧、gaiji alt 書式など。
このうち**ヘッダのエスケープ差**を Quirk `raw_header_metadata` で解消
（byte 一致 17181→17194、+13）。

**指標の注意（重要）**: ハーネスの「改善/悪化」カウンタは構造的同等
（structural equivalence）粒度で、byte 非一致でも構造同等なら悪化に数えない。
**真の互換指標は byte-exact 件数**（results の status=="exact" の数）。
リファクタの挙動不変確認や改善計測は、改善/悪化カウンタだけでなく
byte-exact 件数の増減で判断すること。error（status!=exact/different/equivalent）は
オラクル gz が無い作品で、byte-exact の実効上限は 17509−error になる。

**error の正体（2026-07 調査で確定。旧記述「CRC 不良 zip」は誤り）**: オラクル
build（build_oracle.sh）は参照実装 aozora2html が**終了コード0で完走したときだけ**
gz を作る。error はハーネスが選んだ zip エディションで**参照実装自身が失敗**して
gz が無い作品。56 件の内訳: 記法エラーで停止 42（うち `字下げを閉じようとしま
したが字下げ中ではありません` 34、二重ルビ/著者2回/字詰め・字下げ中に本文終了/
burasage/1バイト記号+後続エラー 8）、**参照実装のクラッシュ（Ruby NoMethodError:
push_block_tag/style_stack）11**、zip に .txt 無し 1、その他 2。同一 work に別
エディション zip（例 233_ruby_2990 と 233_txt_2991）があり片方だけ参照が通る例も
あるが、ハーネスの pair 選択は 1 エディション固定なので基準は変わらない。
つまり error はコンバータの改善対象外（比較対象が存在しない）。

**残差観測の手順**: `results-oracle.jsonl` は diff テキストを持たないので、
各 different 作品でオラクル gz と `aozora2 html` 出力を突き合わせ、最初の
相違の前後を正規化した署名で集計する（実装は当時のスクリプトを参照。
数値→N、非ASCII→1文字に潰すとタグ構造の差だけが残りクラスタになる）。

**幅の数字連結バグの修正（2026-07, ループA）**: burasage と字下げ等の幅
抽出が `extract_number` で部分文字列中の全数字を連結していた
（`３字下げ、地より１字上げ`→31、`７字下げ、２１字詰め`→721 等）。
参照実装の `(\d*)字下げ` に合わせ `extract_number_before` で単位キーワード
直前の数字だけを取るよう修正（burasage +2、字下げ +4）。

**残差の現状（byte-exact 17221/17509 = 98.36%, error 56, different 232）**:
2026-07 のループAで潰した一般バグ: ヘッダのエスケープ（Quirk, +13）、
幅の数字連結（burasage +2 / 字下げ +4）、字下げ幅の漢数字化（+2、校正注記
`一字下げ忘れか？` の誤コマンド化も解消）、ブロック終止の仮名「おわり」受理（+2）。

**burasage per-line モデル移植（2026-07, 段2c 最大リスク着手）**: 参照実装
general_output の per-line indent-stack モデルを移植。真理値表 A〜G を実測し、
以下を修正（合計 +17）:
- 行の包み判定を出力HTML（classify_line）から AST 由来の has_inline_text
  （blank_type 相当）へ。`テキスト［＃地付き］右` のような行中インラインブロックを
  含む行も包める（000072 脚本群、改善19・悪化0）。
- 行中インラインブロックが close_related_blocks で burasage を閉じる不具合を
  is_block=true 限定で修正。close_inline_blocks を包み判定前に line_html へ取り込み。
- コンマなし `折り返してN字下げ` の幅を空に（find_burasage_context を Option 化）。
- burasage は最上位ブロックのときだけ包む（入れ子横組み等の内側を包まない）。

**確定した限界（入れ子ブロック閉じ行の空 burasage div）**: 参照は横組み/小さな文字
等が burasage 内で閉じる行を、閉じタグを String 扱いして空の
`<div class="burasage"></div>` で包む（真理値表 E/G）。これを
`line_html.starts_with("</")` で近似実装したところ悪化162（正常な閉じ行を
誤って包む）で revert。出力HTMLからは String/Multiline を区別できず、
参照の TagObject 型モデルを持たない限り安全に再現できない。残差 different の
かなりの割合がこのエッジ。将来やるなら renderer が per-line のトークン列
（String/Multiline/OnelineIndent の型）を保持する構造へ作り替える必要がある。

different を再クラスタした結果、大きく2群:
- **burasage モデル（約72件）**: no-comma 時の空幅・入れ子ブロック内での
  非包み・ブロック境界の空 burasage div など、参照の per-line indent-stack
  モデル固有の挙動が複数絡む。個別の byte 一致には段2c の全面移植が必要で、
  多数の正常な burasage 作品への回帰リスクが高い。最小再現は
  `［＃ここから折り返して１字下げ］`（no-comma）＋入れ子横組み で得られる。
- **異種の長い尾（約175件）**: 次に目立つ共通群は**前方参照が対象内の
  ruby/gaiji に阻まれて解決失敗し注記化**するもの（000020 `［＃「瀕」は太字］`で
  瀕が ruby 親文字、001393 傍点、001930 superscript、000150 縦中横 等）。
  対象文字列に ruby/gaiji が絡むと前方参照の対象探索が外すのが原因で、
  複数作品に共通するが解決器の対象探索の見直しが要る（次の候補）。
  ほか gaiji 一覧テーブル（000091）、末尾 `<br/>` 数など。

**burasage blank_type クラスタの解消（2026-07 続き, 段2c, byte-exact 17246→17267）**:
参照 TextBuffer#blank_type は「バッファに空でない **String** があれば false（＝
その行を per-line burasage div で包む）」。逆に **Tag オブジェクトしか無い行**は
blank_type true で包まれず `<br />`。どのノードが String を残し、どれが Tag に
取り込むかを実測（`ruby -Ilib bin/aozora2html` に最小再現を通す。CRLF 必須）:
- **String を残す（包む）**: 平文テキスト、明示形縦中横 ［＃縦中横］32［＃終わり］
  （32 は Text として残る＝BlockStart/BlockEnd＋Text）、装飾/文字サイズ/縦中横が
  対象の**外**に平文を持つ行、Accent の後続文字（〔e's〕の s）、unicode 化する外字。
- **Tag に取り込む（包まない）**: 画像 Node::Img、ルビ Node::Ruby（親文字を取り込む）、
  装飾 Node::Style（傍点/傍線/太字/斜体、対象をタグに取り込む）、文字サイズ
  Node::FontSize（「N段階大きな文字」）、前方参照縦中横 Node::Tcy（「32」は縦中横）、
  画像化する外字 Node::Img、注記 Node::Note、ブロック制御。
これらを `nodes_have_inline_text`（renderer.rs）の false 分岐に列挙。前方参照系は
対象を単一ノードに畳んで親文字を上位 Text から消すので「行全体がそれ**だけ**」の
ときのみ包まれない（対象の外に平文があれば Text が残り従来どおり包む）。各ノード
追加ごとにオラクルで悪化0を確認しコミット。あわせて修正した burasage の別件:
- **同行中見出し＋本文**（045387）: 行頭 `<h4 dogyo-naka-midashi>` の後に本文が
  続く行は blank_type false なので包む。包み判定 starts_with_block を `<div` のみに
  絞り `<h` 始まりを除外基準から外した（chitsuki/jisage の行 div は `<div` 始まりで
  依然包まない）。
- **画像行**（001699 等）: Node::Img は Tag なので包まず `<img/><br />`。
- **読点必須**（000061）: PAT_ORIKAESHI_JISAGE2 = `(\d*)字下げ、折り返して(\d*)字下げ` は
  読点（、）必須。`１０字下げ折り返して１７字下げ`（読点なし）は不一致で
  margin-left 空・text-indent 0。try_parse_burasage の分岐を `first_part.ends_with("字下げ、")`
  ＆ `second_part.contains("字下げ")` に厳格化。
- **折り返し最優先ディスパッチ**（001675 等, +4）: 参照 dispatch_aozora_command は
  ORIKAESHI（折り返して）を他の全分岐より先に apply_burasage へ回す。「ここから」の
  無い ［＃改行天付き、折り返して５字下げ］ もぶら下げ。parse_command 冒頭で
  折り返して を検出し try_parse_burasage（pub 化）へ。従来は line_indent に落ちて
  jisage ブロックにしていた。

**残差の現状（byte-exact 17267/17509 = 98.62%, error 56, different 185）**。
burasage-different は 24 件まで減。残りは個別の別クラスタ:
- **入れ子ブロック閉じ行の空 burasage div**（真理値表 E/G, 上述の確定した限界）。
- **@terprip=false 伝播**（000072『三右衛門手記』等 54444）: 新しい burasage/jisage が
  既存 burasage を implicit_close するとき @terprip=false になり、続く空行が `<br />`
  ではなく `</div>` を出す（general_output L526-535 の else 分岐）。1行差だが per-line
  モデルの状態伝播が要る。
- **深い入れ子**（jizume/keigakomi の中の burasage、060192/057368）: 中間の
  div.jizume/keigakomi 層の有無。
- **前方参照が対象内 ruby/gaiji で解決失敗し注記化**（長い尾の主群、052468/003597/047180
  等）。対象探索の見直しが要る。gaiji 一覧テーブル、末尾 br 数など散在。

**外字・アクセント・前方参照クラスタの解消（2026-07 続き, ループA, byte-exact 17267→17314）**:
真の初回相違はオラクル gz と `aozora2 html` の直接行 diff で取る（構造 diff の
先頭は桁ずれのカスケードで真因でないことが多い）。解消した一般バグ:
- **句点コードの面制約**（000048 等, +3）: 参照 kuten2png は `/[12]-\d{1,2}-\d{1,2}/`
  にしか一致させない。面は1桁の1・2、区点は1〜2桁。従来 extract_jis_code は
  `\d+-\d+-\d+` を無条件に拾い、`24-1-3`（底本位置参照で面24は無効）まで画像化
  していた。無効面は None → ※＋注記のまま。gaiji.rs を書き換え。
- **外字入り訓点送り仮名**（000603 等, +4）: 参照 PAT_OKURIGANA = `^（(.+)）$` は
  長さ無制限で、内側は注記と同じ TagParser で描画（外字→img）。従来は括弧内
  10文字までに絞り、［＃（※［＃「低のつくり」…］）］ を取りこぼして注記化。
  try_parse_okurigana の長さ制限撤廃＋ Node::Okurigana の描画を render_note_content に。
- **アクセント内の外字消失**（多数, +31 の大物）: Token::Accent 処理が全子ノードを
  to_text() で平坦化してから parse_accent していたため、内側の外字 ※［＃…］ が
  記述文字列に潰れ生テキスト化していた（〔…au ※［＃ローマ数字19、37-下-11］e
  siècle〕）。Text ノードだけにアクセント変換を掛け、外字等は Node のまま残す。
- **前方参照の接尾辞一致**（+5）: 参照 search_front_reference は対象を**バッファ末尾の
  接尾辞**としてのみ照合する（last_string.match?(/target$/)、複数要素へ後方展開）。
  途中一致は取らない。従来 find_target_in_preceding は単一 Text 内を rfind で途中
  一致まで拾い `をツさん［＃「をツ」に傍点］` を過剰解決していた。単一ノード照合を
  ends_with に変更（接尾辞なら rfind と同結果なので悪化なし＝過剰解決だけ除く）。
- **複数ノード解決後の参照 skip**（000289 等日記形式, +4）: apply_resolution の
  MultiNodeExact が対象ノードを1つに畳んで nodes_removed-1 個減らすのに *i を
  更新せず continue していたため、直後の未解決参照を読み飛ばしていた
  （ルビ入り同行中見出しの直後の日付縦中横 ４・19［＃「19」は縦中横］ が注記化）。
  `*i = new_i` で位置補正。

**残差の現状（byte-exact 17314/17509 = 98.89%, error 56, different 143）**。
真の初回相違でクラスタすると残りは:
- **前方参照の複合対象**（未解決の主群）: 対象が ruby/subscript を含む
  （`「Ｘ１」は縦中横` の１が subscript）や二重対象（`「のれん」と「ねうち」に傍点`）だと
  解決器が対象を組み立てられず注記化。参照の search_front_reference の複数要素
  後方展開（ReferenceMentioned も含む）を忠実移植すれば潰せるが、最も使われる
  経路なので慎重に段を割ること。
- **@terprip=false 伝播**（54444 系, 上述）。
- **深い入れ子**（jizume/keigakomi 内 burasage, 060192/057368）。
- **入れ子ブロック閉じ行の空 burasage div**（真理値表 E/G の確定した限界）。
- gaiji 一覧テーブル、末尾 br 数、二重外字 ※.*※ など散在。

**前方参照リゾルバの忠実移植（2026-07 続き, ループA, byte-exact 17314→17335, 99%突破）**:
「最近の修正から予想できる同型バグ」を起点に着手。
- **返り点の文字集合/長さ**（潜在, 悪化0）: 参照 PAT_KAERITEN = `^[一二三四五六七八九十
  レ上中下甲乙丙丁天地人]+$`。is_kaeriten は五〜十欠落＋参照に無い4文字制限があった。補完・撤廃。
- **search_front_reference 移植**（+12）: find_target_in_preceding が両ループとも全ノードを
  走査し、参照の「バッファ末尾から連続した接尾辞スパン」に不一致だった。過剰解決
  （末尾以外に一致。`をツさん［＃「をツ」に傍点］`）と過少解決（最古ノードを分割
  できず対象内に既解決ノードを含むと組めない。`「Ｘ１」は縦中横` の Ｘ＋下付き１）の
  両方が起きていた。参照を移植: 末尾から連続要素を消費し、String は末尾一致で分割
  （前半を残す）／対象がその要素で終われば残りを再帰、ReferenceMentioned（ルビ・
  装飾 Decorate・文字サイズ・縦中横 Dir・罫囲み・横組み・キャプション・見出し）は
  内部テキストが対象末尾に一致すれば要素まるごと子に取り込む、空文字列は読み飛ばし、
  画像/外字/訓点等に当たれば失敗。SplitInfo/apply_resolution/find_target_in_preceding を
  FrontRefMatch/search_front_reference/apply_front_reference に置換。
- **コンテナ子内の前方参照**（+9）: ｜瀕［＃「瀕」は太字］《ひん》 のようにルビ親文字の
  内側にある前方参照は、参照が @ruby_buf を対象にルビを閉じる前に解決する。こちらは
  ルビ親文字解決後に親文字ノード列へ UnresolvedReference が残るので、resolve_style_
  references をコンテナ（ルビ/装飾/文字サイズ/縦中横/罫囲み/横組み/キャプション/
  見出し/割書き）の子へ再帰させて同スコープ内で解決する。

**残差の現状（byte-exact 17335/17509 = 99.01%, error 56, different 126→減少中）**。
残りの前方参照系（notes 化）に残るのは:
- **二重対象**（`「のれん」と「ねうち」に傍点` → 参照は ねうち のみ強調）。PAT_FRONTREF の
  複雑な入れ子照合の再現が要る。
- **校訂注記型**（`「X」は底本では「Y」`, `「X」はママ`, `「X」に「（ママ）」の注記`, 傍記
  `「X」に「×」の注記`）: これは注記のままだが出力形（対象への傍点・傍記マーカーや
  注記本文の描画）が違う。
- 対象内に ruby/gaiji を含む複合（`「銅※［…］」の左にルビ` 等）。
ほか: @terprip=false 伝播（54444）、深い入れ子（jizume/keigakomi 内 burasage）、
入れ子ブロック閉じ行の空 burasage div（確定した限界）、gaiji 一覧テーブル。

**注記系クラスタ（2026-07 続き, ループA, byte-exact 17335→17342, 99.05%）**:
- **注記ルビの部分一致**（+5）: 参照 PAT_CHUUKI = `/「(.+?)」の注記/` は spec への
  部分一致で「の注記」の後ろに続きがあってもよい（`「（ママ）」の注記、正しくは「十三」`／
  `「（ママ）」の注記がある`）。try_parse_annotation_ruby の `ends_with("の注記")` を
  `contains("」の注記")` に緩和し `「X」に「Y」の注記` を ruby(base=X, rt=Y) にする。
  ただし入れ子注記（`「書卓が」は底本では「□□［＃「□□」に「二字欠落」の注記］が」`）は
  参照が入れ子 ［＃…］ を先に解決するため外側は一致しない→抽出注記に ［＃ を含めば除外
  （悪化1を回避した実例。平坦文字列照合の限界を入れ子検出で補う）。
- **注記の前後空白保持**（+2）: 参照 apply_rest_notes は命令文字列をそのまま
  EditorNote にするので末尾全角空白（`「降った来た」はママ　`）も残る。parse_command
  冒頭の trim() で落としていたのを、最終フォールバックの注記だけ原文（untrimmed）に。

**残差の現状（byte-exact 17342/17509 = 99.05%, error 56, equivalent 1, different 110）**。
error 56 は参照実装が失敗しオラクルが無い作品（内訳は §指標の注意を参照）。実効上限 約 17453。残りの
different 110 は個別色の強い長い尾:
- **二重対象**（`「のれん」と「ねうち」に傍点` → 参照は ねうち のみ）。PAT_FRONTREF の
  入れ子照合の再現が要る。
- **傍記**（`「X」に「×」の注記` の傍記マーカー描画）、校訂注記の一部で出力形が違う。
- **｜（ルビ親文字区切り）の多重**（42602: `今日｜民族観念…悲憤｜慷慨《…》` を巨大ルビに
  誤結合）。参照は 《》 直前の ｜ を基点にする。
- 対象内に ruby/gaiji を含む複合ルビ・傍記。
- @terprip=false 伝播（54444）、深い入れ子（jizume/keigakomi 内 burasage）、
  入れ子ブロック閉じ行の空 burasage div（確定した限界）、gaiji 一覧テーブル。

**ルビ親文字区切り ｜ 系（2026-07 続き, ループA, byte-exact 17342→17344）**:
- **多重 ｜**（42602, +1）: 参照 RubyBuffer は ｜ のたびに dump_into して protected を
  立て直すので、《》直前の**最後の** ｜ からが親文字。それより前の ｜… 区間は ｜ を
  リテラルに残して本文へ。read_prefixed_ruby は ｜ から最初の 《 まで丸ごと親文字に
  していたため `今日｜民族観念［＃「民族観念」に傍点］と呼ぶ…悲憤｜慷慨《こうがい》` を
  巨大ルビに誤結合していた。最初の ｜ の後にトップレベル（コマンド ［…］ の外）で
  もう一つ ｜ があれば最初の ｜ をリテラルにして内容を再トークナイズに委ねる。
- **関連仕様の確認で発見: ヘッダのルビ/｜ 除去**（+1）: 参照 parse_header は各ヘッダ行に
  `gsub!(RUBY_PREFIX,''); gsub!(PAT_RUBY,'')` をかけ、題名/著者等から ｜ とルビ 《…》 を
  落とす（PAT_RUBY = /《.*?》/ 非貪欲）。従来は生のまま <title>/<h1>/DC.Title に出て
  いた。strip_header_ruby を追加。あわせて確認: 非 ｜ ルビの親文字 char_type グルーピング
  （あの人《ひと》→人、本町３丁目《…》→丁目）は参照一致、char_type 範囲も SJIS
  コーパスでは実質等価（U+4E00-9FFF は [亜-熙] の上位集合だが非 SJIS 文字は登場しない）。

**画像 alt のアクセント除去（2026-07 続き, ループA, +3）**: 参照実装の画像 alt は
TagParser の @raw（read_char で読んだ生文字の蓄積）由来。アクセント 〔...〕 は
専用 AccentParser が別ストリームで読むため中身が @raw に入らず、read_char で読んだ
開き 〔 だけ残って中身と閉じ 〕 が落ちる（`折〔衷〕派`→`折〔派`、`〔金銭出納録〕の表`→
`〔の表`）。strip_accent_from_alt を try_parse_image に追加して再現。参照実装の
実装都合（サブパーサが @raw に寄与しない）に起因する挙動で、alt から文字が落ちる
（実質バグ）だが互換のため既定で再現。対応する 〕 が無ければ消費されず残す。

**前方参照の返り点/送り仮名＋外字altの「※」は除去（2026-07 続き, ループA, +5）**:
参照 exec_frontref_command はキャプションの後に 返り点(KAERITEN_COMMAND)・訓点
送り仮名(KUNTEN_OKURIGANA_COMMAND) を前方参照として処理する（`「レ」は返り点`→
`<sub class="kaeriten">レ</sub>`）。InlineKind に Kaeriten/Okurigana を追加、
try_parse_inline_element でキャプションの後に判定。あわせて外字 alt の
`「※」は`/`「※」の`（参照 PAT_KUTEN = /「※」[は|の]/）を strip_kuten_prefix で
除去（`※(「※」は「竹かんむり＋弄」…)`→`※(「竹かんむり＋弄」…)`）。

**前方参照バックトラックの忠実移植（2026-07 続き, ループA, +3, 解決済み）**: 参照
PAT_FRONTREF は文字列全体への正規表現照合。(a) 最初の 「 で対象が不均衡
（`「「古典的`）なら次の 「 から再試行、(b) 対象・接続詞が成立した位置では確定し
spec がスタイル不成立でも注記化して再試行しない、(c) 入れ子 ［…］ の中の 「 は
（参照では入れ子が先に解決されるため）対象候補にしない。最初に「位置ごとに単純
再試行」だけ実装したら**悪化4**（`２文字目の「i」は下付き小文字、４文字目の「i」は
上付き小文字` を2つ目の 「 で解決／`「う［＃「う」に「ママ」の注記］」はママ` の内側
注記を誤解決）。真因を実測し、(b)「find_target_end が成立する最初の 「 で確定」と
(c) skip_bracketed による入れ子スキップを加えて悪化0で解決（52529/54228 ほか）。
教訓: 参照の正規表現は「マッチ成立位置では確定・スタイル不成立でも注記」であり、
スタイル不成立を「不一致」と混同して再試行すると過剰解決になる。

**外字・ルビ親文字の細則（2026-07 続き, ループA, +4）**:
- **非0213外字ガード**（004603, +1）: 参照 kuten2png は説明に NON_0213_GAIJI
  「非0213外字」を含むと、[12]-\d{1,2}-\d{1,2} 風の水準参照があっても画像化せず
  注記化する。extract_jis_code の先頭で 非0213外字 を含めば None に。
- **ルビ親文字の hankaku_terminate 結合**（000891 等, +3）: 参照 RubyBuffer#push_char は
  hankaku_terminate（. ; " ? ! )）を直前が hankaku のとき同グループにする
  （Fig.《…》→ 親文字 Fig.）。extract_ruby_base で末尾が hankaku_terminate かつ
  直前が hankaku なら hankaku 連ごと親文字に含める（あ.→. / Fig..→. は最後の1つ）。

**残差の現状（byte-exact 17359/17509 = 99.14%, error 56, equivalent 1, different 93）**。
残る different の主な難所（多くは参照実装のバグ/実装都合の再現が必要）:
- **外字入りルビ親文字＋底本注記**（004995/057416/001452）: `孫子※［＃…］《そんしばく》`
  ＋直後の底本注記で、参照は親文字を失い空 alt の img だけ出す（参照バグ）。再現が高難度。
- 傍記・二重対象（`「A」と「B」に傍点`）、対象内 ruby/gaiji の複合ルビ。
- 図一覧テーブルの集約（046820）、キャプション付き図の一覧（018371）。
- @terprip=false 伝播（054444）、深い入れ子（jizume/keigakomi 内 burasage）、
  入れ子ブロック閉じ行の空 burasage div（確定した限界）、gaiji 一覧テーブル。
error 56 は参照実装が失敗しオラクルが無い作品（内訳は §指標の注意を参照）。実効上限 約 17453。

次の作業の選択肢（どれもオラクル byte-exact で各段検証。真の初回相違は gz と
出力の直接行 diff で取ること。修正時は関連仕様も確認する）:
1. 傍記マーカー・校訂注記の出力形。
2. 二重対象の前方参照（`「A」と「B」に傍点`）。
3. @terprip=false 伝播（54444 系）。理解済みだが per-line 状態伝播が要りリスク中。
4. ループD 定常運用（コーパス/注記一覧更新・未対応記法の集計）に戻る。
